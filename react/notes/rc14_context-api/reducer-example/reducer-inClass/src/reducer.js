const initialState = {
    loading: false,
    catImage: "",
    error: "",
}

const reducer = (initialState, action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}